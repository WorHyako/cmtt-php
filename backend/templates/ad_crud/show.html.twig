{% extends 'base.html.twig' %}

{% block title %}Ad{% endblock %}

{% block body %}
    <h1>Ad</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ad.id }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ ad.price }}</td>
            </tr>
            <tr>
                <th>Banner</th>
                <td>{{ ad.banner }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ ad.text }}</td>
            </tr>
            <tr>
                <th>ShowLimit</th>
                <td>{{ ad.showLimit }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ad_crud_index') }}">back to list</a>

    <a href="{{ path('ad_crud_edit', {'id': ad.id}) }}">edit</a>

    {{ include('ad_crud/_delete_form.html.twig') }}
{% endblock %}
